# idk-chess-5d

Experimental chess firmware that currently reuses the upgraded `idk-chess` engine and UI.

- Faster/deeper default AI than the base firmware
- Kept as a separate firmware so custom 5D rules can be added later without breaking classic chess

Current note:
- Until the exact 5D rules are defined, this firmware runs the upgraded classic engine under a separate title.
